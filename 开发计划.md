# SWE-Agent 变体项目 — 四周详细开发计划

## Context

项目从零开始，目前仅有 `计划.txt` 和空的 `readme.md`。目标是四周内构建一个基于 FSM 的自动化代码修复 Agent 系统，能在 SWE-bench-lite 子集上进行真实评测。本计划将每周拆解为可执行的每日/每阶段任务，并定义每周末必须达到的交付标准。

---

## 项目目录结构规划

```
xiangmu1/
├── swe_agent/
│   ├── __init__.py
│   ├── main.py              # Agent 入口
│   ├── sandbox/
│   │   ├── __init__.py
│   │   └── docker_runner.py # Docker 沙盒执行器
│   ├── ast_view/
│   │   ├── __init__.py
│   │   ├── skeleton.py      # SkeletonTree 类
│   │   └── function_map.py  # AST 函数行号提取
│   ├── fsm/
│   │   ├── __init__.py
│   │   └── agent_fsm.py     # 状态机定义与流转
│   ├── llm/
│   │   ├── __init__.py
│   │   └── client.py        # DeepSeek API 封装
│   ├── tools/
│   │   ├── __init__.py
│   │   ├── schemas.py       # Pydantic 工具 Schema
│   │   └── registry.py      # 工具注册与调度
│   └── utils/
│       ├── __init__.py
│       ├── logger.py         # 结构化日志
│       └── token_counter.py  # Token 统计
├── tests/
│   ├── test_sandbox.py
│   ├── test_ast.py
│   ├── test_fsm.py
│   └── test_tools.py
├── examples/
│   ├── bug_return_value.py   # 示例 bug 1
│   └── bug_undefined_var.py  # 示例 bug 2
├── eval/
│   ├── swe_bench_subset/     # 评测数据
│   ├── run_eval.py           # 批量评测脚本
│   └── results/              # 结果输出
├── requirements.txt
├── Dockerfile
├── .env.example
└── readme.md
```

---

## 第 1 周：基础补强 + 最小闭环（直线版）

### 目标
端到端跑通单文件 Python bug 修复的直线流程。

### 任务清单

#### Day 1-2：Docker 沙盒基础
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 安装 Docker + docker-py，编写 `run_in_docker(code_dir, command)` | P0 | 4h | 函数返回 stdout/stderr/exit_code |
| 安全参数：mem_limit=1g, read_only, network_disabled | P0 | 2h | Docker 配置参数写入 `docker_runner.py` |
| 单元测试：`test_sandbox.py` 验证基本执行 | P0 | 1h | pytest 通过 |

**关键文件**：`swe_agent/sandbox/docker_runner.py`、`tests/test_sandbox.py`

#### Day 3：AST 函数行号提取
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 实现 `get_function_line_map(filepath)` → `{func_name: (start, end)}` | P0 | 3h | 能正确提取顶层函数行号 |
| 支持类内方法提取 | P1 | 2h | 返回 `ClassName.method_name` 格式 |
| 单元测试：`test_ast.py` | P0 | 1h | pytest 通过 |

**关键文件**：`swe_agent/ast_view/function_map.py`、`tests/test_ast.py`

#### Day 4-5：DeepSeek API + Function Calling
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 使用 OpenAI SDK 接入 DeepSeek（base_url 改写） | P0 | 2h | 能正常调用并返回响应 |
| 定义工具 `search_function(name: str)`，让模型返回 JSON | P0 | 3h | Function Calling 正确解析 |
| 封装 LLM 客户端类 | P0 | 2h | `llm/client.py` 可调用 |

**关键文件**：`swe_agent/llm/client.py`、`.env.example`

#### Day 6：直线流程集成
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 用 if-else 实现 locate → patch → test → repeat（最多 2 次） | P0 | 4h | 主流程可运行 |
| 修复后自动保存文件 | P0 | 1h | 修复结果写入文件 |

**关键文件**：`swe_agent/main.py`

#### Day 7：示例 bug + 验证
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 编写两个示例 bug 文件 | P0 | 2h | `examples/` 下两个 bug 文件 |
| 端到端测试：至少修复一个 bug | P0 | 2h | 命令行输出 SUCCESS |
| 基础日志（print 输出每步信息） | P1 | 1h | 可见 prompt/tool_calls/test 输出 |

**关键文件**：`examples/bug_return_value.py`、`examples/bug_undefined_var.py`

### 第 1 周交付标准（Week 1 Milestone）
- [ ] `docker_runner.py` 可在隔离容器内执行命令并返回结果
- [ ] `function_map.py` 可提取单文件函数行号
- [ ] DeepSeek API 调用正常，Function Calling 可解析
- [ ] `main.py` 能端到端跑通单文件 bug 修复
- [ ] 至少修复 1 个示例 bug 并通过测试

---

## 第 2 周：AST 上下文压缩 + 完整 FSM 骨架

### 目标
支持多文件项目，用骨架树压缩上下文，用状态机重构流程。

### 任务清单

#### Day 8-9：SkeletonTree 类
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 递归遍历目录，提取所有 .py 文件的函数/类签名 | P0 | 4h | 输出骨架文本 |
| 实现 `expand_function(file_path, func_name)` 返回完整源码 | P0 | 3h | 可展开指定函数 |
| LRU 缓存（最多缓存 5 个展开函数） | P0 | 2h | 重复调用不重复读文件 |

**关键文件**：`swe_agent/ast_view/skeleton.py`

#### Day 10-11：FSM 状态机
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 使用 `transitions` 库定义 6 个状态（INIT/LOCATE/PATCH/TEST/SUCCESS/FAIL） | P0 | 3h | 状态流转图可运行 |
| 编写每个状态的 `on_enter` 回调 | P0 | 4h | 回调函数触发 LLM 调用 |
| 将第 1 周的 if-else 逻辑迁移到状态机 | P0 | 3h | 功能等价且可运行 |

**关键文件**：`swe_agent/fsm/agent_fsm.py`

#### Day 12：多文件项目测试
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 构造 3 文件测试项目（含跨文件引用 bug） | P0 | 3h | 测试项目可运行 |
| 对比骨架压缩前后的 prompt token 数 | P1 | 2h | 目标减少 50%+ |
| Token 统计工具 | P1 | 2h | `token_counter.py` 输出统计 |

**关键文件**：`swe_agent/utils/token_counter.py`

#### Day 13-14：集成与调试
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 端到端多文件 bug 修复测试 | P0 | 4h | 至少修复 1 个多文件 bug |
| 修复状态机流转中的边界情况 | P0 | 3h | 无卡死/无限循环 |
| 支持类内方法展开 | P2 | 2h | 可选，时间允许则做 |

### 第 2 周交付标准（Week 2 Milestone）
- [ ] `skeleton.py` 可生成多文件项目的骨架树并展开函数
- [ ] FSM 状态机驱动的 Agent 主流程可运行
- [ ] 能处理多文件项目（3+ 文件）
- [ ] Token 统计显示骨架压缩效果 ≥ 50%
- [ ] 至少修复 1 个多文件示例 bug

---

## 第 3 周：防死循环 + Docker 加固 + 约束解码

### 目标
解决 Agent 重复调用、无限循环问题，增强输出可靠性与安全性。

### 任务清单

#### Day 15-16：防死循环机制
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| Watchdog 计数器：记录每状态进入次数 | P0 | 3h | 同状态同工具 ≥ 3 次触发回退 |
| Checkpoint 机制：PATCH 前备份代码快照 | P0 | 3h | 回退时恢复快照 |
| 指数退避重试：JSON 解析失败时 2^retry 秒重试 | P0 | 2h | 最多重试 3 次 |

**关键文件**：`swe_agent/fsm/agent_fsm.py`（在现有 FSM 中增加逻辑）

#### Day 17-18：Docker 安全加固
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 完善安全配置：network_disabled, mem_limit, read_only, no privileged | P0 | 2h | 配置参数齐全 |
| 危险命令黑名单：拦截 rm -rf /、sudo、chmod 777 等 | P1 | 2h | 命中黑名单时拒绝执行 |
| 集成测试：模拟恶意命令 | P1 | 2h | 黑名单拦截生效 |

**关键文件**：`swe_agent/sandbox/docker_runner.py`

#### Day 19-20：约束解码 + Pydantic Schema
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 使用 `outlines` 或 `jsonformer` 强制 JSON 输出 | P0 | 4h | 模型输出始终为合法 JSON |
| 定义所有工具的 Pydantic Schema（Search/Expand/Edit） | P0 | 3h | Schema 校验通过 |
| 非法输出拒绝并重试（附错误信息） | P0 | 2h | 系统不会因脏输出崩溃 |

**关键文件**：`swe_agent/tools/schemas.py`、`swe_agent/tools/registry.py`

#### Day 21：稳定性测试
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 模拟模型返回非法 JSON，验证系统不崩溃 | P0 | 2h | 异常输入被正确处理 |
| 结构化日志（structlog JSON 格式） | P2 | 2h | 日志可机器解析 |
| 端到端回归测试 | P0 | 2h | 第 1/2 周功能不退化 |

**关键文件**：`swe_agent/utils/logger.py`

### 第 3 周交付标准（Week 3 Milestone）
- [ ] Watchdog + Checkpoint + 指数退避机制可正常工作
- [ ] Docker 沙盒安全配置完整，危险命令被拦截
- [ ] 约束解码确保输出始终为合法 JSON
- [ ] 所有工具调用经过 Pydantic 校验
- [ ] 模拟异常输入不会导致系统崩溃
- [ ] 端到端回归测试通过

---

## 第 4 周：评估 + 文档 + 简历材料

### 目标
在 SWE-bench-lite 子集上跑出数据，产出可展示的成果。

### 任务清单

#### Day 22-23：评测数据准备与运行
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 下载 SWE-bench-lite，筛选 5-10 个 Python 实例 | P0 | 3h | 子集数据就绪 |
| 编写批量评测脚本 `run_eval.py` | P0 | 4h | 可自动运行所有实例 |
| 记录每实例结果：成功/失败、状态转换次数、token 消耗、耗时 | P0 | 2h | 输出 JSON 结果文件 |

**关键文件**：`eval/run_eval.py`、`eval/results/`

#### Day 24：失败分析
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 失败分类脚本：定位错误/修复错误/超时/死循环 | P1 | 3h | 统计结果输出 |
| 挑选 1-2 个典型失败 case 做根因分析 | P1 | 2h | 分析笔记完成 |

#### Day 25-26：文档与展示
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| GitHub README：项目介绍、架构图（Mermaid）、快速开始、评估结果、技术难点 | P0 | 4h | README 完整且可读 |
| 更新简历：替换为实测数据 | P0 | 2h | 简历亮点段落完成 |
| 技术博客（3000 字，知乎/掘金） | P1 | 4h | 博客草稿完成 |

#### Day 27-28：收尾与优化
| 任务 | 优先级 | 预计耗时 | 完成标志 |
|------|--------|----------|----------|
| 演示视频/GIF（1 分钟内，LICEcap/OBS） | P1 | 3h | 录屏文件就绪 |
| 针对 1-2 个失败 case 做改进（修改 prompt/增加工具） | P2 | 3h | 记录改进效果 |
| Git 仓库整理：提交历史、.gitignore、LICENSE | P0 | 1h | 仓库可公开 |

### 第 4 周交付标准（Week 4 Milestone）
- [ ] SWE-bench-lite 子集（5-10 实例）评测结果 JSON
- [ ] 失败分类统计与根因分析
- [ ] 完整 GitHub README（含架构图、评估结果、技术难点）
- [ ] 简历亮点段落（含实测指标）
- [ ] 技术博客草稿
- [ ] 演示视频/GIF

---

## 各周完成度检查表

### 总览

| 周次 | 核心交付物 | 预计工时 | 验收标准 |
|------|-----------|----------|----------|
| Week 1 | 单文件 bug 修复闭环 | 25-30h | 端到端修复 ≥ 1 个示例 bug |
| Week 2 | 多文件 + FSM 状态机 | 30-35h | 多文件修复 + token 压缩 ≥ 50% |
| Week 3 | 防死循环 + 安全沙盒 + 约束解码 | 30-35h | 异常输入不崩溃，watchdog 生效 |
| Week 4 | 评测 + 文档 + 展示材料 | 25-30h | README + 评测结果 + 简历就绪 |

### 每日自检问题
1. 今天的核心功能是否可运行？（不要等到周末才验证）
2. 是否写了对应的单元测试？
3. 是否有未处理的异常或 TODO？
4. 明天的任务是否依赖今天未完成的部分？

---

## 技术栈确认

| 用途 | 库/工具 | 备注 |
|------|---------|------|
| LLM 调用 | openai SDK (DeepSeek 端点) | base_url 改写 |
| 状态机 | transitions | PyPI: transitions |
| 沙盒 | docker-py | Python Docker SDK |
| AST 解析 | ast (标准库) | 无需额外安装 |
| Token 统计 | tiktoken | 近似统计 |
| 约束解码 | outlines 或 jsonformer | 二选一，优先 outlines |
| Schema 校验 | pydantic | v2 |
| 日志 | structlog 或 logging | JSON 格式 |
| 评测 | SWE-bench-lite | 官方 GitHub |

---

## 风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|----------|
| DeepSeek API 不稳定/限流 | 中 | 阻塞测试 | 准备备选 API（如通义千问） |
| Docker 环境配置问题 | 中 | 阻塞沙盒 | 提前在 Day 1 验证 Docker 可用 |
| outlines 库兼容性问题 | 低 | 阻塞约束解码 | 退回 jsonformer 或手写正则拦截 |
| SWE-bench-lite 实例太难 | 中 | 评测结果差 | 筛选最简单的实例，降低预期 |
| FSM 状态机边界情况多 | 高 | 调试耗时 | 预留 Day 13-14 专门调试 |
