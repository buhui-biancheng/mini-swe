# 官方模式 token 优化方案（2026-08-14）

> 背景：官方模式（OFFICIAL=1，无 test_patch）在 flask-5063 / pytest-7168 实测单实例烧 7.5-8M token（96% 是 cache-read 重发，成本 0.5 元左右）。瓶颈不在"贵"，在"重发次数 × 重发体量"。本文档是优化路线图。

---

## 目标

把官方模式单实例 token 从 **7.5M** 压到 **≤3M**（预算带内），同时保持修复成功率不降（flask-5063 已能产出正确实现）。

---

## ⭐ 方向 0：修交卷门控 bug（最高优先级，pytest-7168 实证 24M 的根因）

**实测**：pytest-7168 跑 24.4M token、108 次状态循环、255 次 LLM 调用后仍不交卷，agent 每轮都回"我已完成修复"但 FSM 就是不提交。

**根因**：`llm/client.py` 的 `chat_with_tools`：
```python
if not response.tool_calls:
    return response.content or "", conversation   # 返回时【没 append】最终 assistant 回复
```
append 发生在无工具检查之后 → `conversation[-1]` 永远是 tool result / user 消息，`role != "assistant"`。而 `agent_fsm.py` 的交卷门控判断 `conversation[-1].role == "assistant"` → 永远 False → **永不提交 → locate→patch→check→test 无限循环**。

**验证盲区**：mock 测试的 `chat_with_tools` 把最终回复 append 了，与真实实现语义不一致 → mock 过、真跑崩。

**修复**：`chat_with_tools` 在无工具返回前 append 最终 assistant 回复；或门控直接查 `final_response`/response.tool_calls。
**预估收益**：循环消失 → token 直接砍掉 95%（24M → ~1M）。

---

## 方向 A：注入图上下文瘦身（最大杠杆）

**现状**：DP 模式 `_graph_context_text()` 注入 L-1 文件级先验 + L0 摘要（节点数/边数/高入度 Top-10）。对 pytest 这种大仓库（500+ 文件），L-1 文件级先验**每轮固定几万 token**，×~90 轮 = 数百万浪费。

**方案**：
- **只保留 L-1 文件级先验**（粗准大地图，文件→函数清单）
- **去掉 L0 摘要注入**（节点/边统计 + 高入度 Top-10——agent 用不上）
- **函数级/行级详情一律按需**：view_file（附带本文件符号清单）、search_function 由 agent 自己查
- L1 邻域不再整体注入，改由 view_file 按需附带（已在 registry 实现）
- 预估：每轮省 1-3 万 token，总计省 **100-300 万**

## 方向 B：减少 API 调用次数（次大杠杆）

**现状**：`chat_with_tools` 每轮工具调用 = 一次完整 `self.chat`（无状态 API 全量重发历史）。pytest 41 工具调用 → 91 次 API 调用。

**方案**：
- `chat_with_tools` 内部**合并轮次**：一次 LLM 回复带多个 tool_use 时，全部执行完再发下一轮（已是如此？需确认）；重点是引导 agent **一次回复多做几件事**（改多处/读多个文件）
- 工具输出截断 4K→2K；历史裁剪 `MAX_HISTORY_CHARS` 800K→400K
- prompt 加"批量动作"偏向：一次调用里并行完成多个独立工具调用
- 预估：API 调用次数砍半 → token 砍半

## 方向 C：图编码为 XY 坐标 / one-hot 向量

**想法**：把图节点映射到向量空间（XY 坐标），按对应项**置 0/1** 表示图结构（邻接/边关系），一次性注入紧凑表示；详细信息（函数体/行号/实现）由 agent 按需查询。

**优点**：图结构信息密度高、token 占用远小于文本列表。
**需设计**：
- 节点→坐标映射：按文件/目录布局？按图嵌入？
- one-hot 维度过大的截断/降维策略
- 与 view_file / search_function 按需查询的衔接（agent 先看紧凑图，再查细节）
- **风险**：抽象坐标表示对 LLM 的可读性/可用性需实验验证（可能不如文本列表直观）

---

## 成功标准

- [ ] 单实例 token ≤ 3M（从 7.5M 起）
- [ ] 修复成功率不降（flask-5063 仍出正确实现）
- [ ] 新鲜输入 token（真花钱的部分）显著下降
- [ ] 成本 ≤ 0.15 元/实例

## 执行时机

- **pytest-7168 双跑结束后**开始
- 按 **A → B → C** 顺序，每步改完跑 flask-5063 官方模式冒烟验证再推进
- 每步留基线数据（token/成本/时间/是否修复）做对照

## 待确认

1. C 的 XY/one-hot 表示细节（维度、布局策略、实验验证方法）
2. B 的批量动作引导方式（prompt 层 vs 工具层）
3. A 去掉 L0 后，是否需要保留"影响面"这种查询能力（按需）

---

## 远期架构：微内核 + 插件（2026-08-14 讨论，暂不实现）

用户问是否搞成其他 harness 那样的微内核 + 插件架构。结论：**有必要，但现在不做**——项目仍在研究/评测期，核心在快速变化，插件化会拖慢迭代。但要**先把边界定好，防止架构锁死**。

**插件边界（将来可插的四个口）**：
1. **工具注册**：`ToolRegistry` 已经是 dict 分发——未来改成按接口注册（新增工具不用改核心）。
2. **FSM 状态回调**：`transitions` 库已支持 on_enter/on_exit 钩子——未来可挂自定义策略（如不同的 locate 策略、新的测试反馈处理）。
3. **图 provider**：`GraphBuilder` 抽象出接口——未来可换 Tree-sitter / 新语言 / 外部图源，不动查询层。
4. **Prompt 覆盖**：`PromptManager` 已按状态拼 prompt——未来可支持外部 prompt 模板覆盖。

**判断**：先保证"这四处别用死硬编码写死"（现在是够松的），等有第二个消费者（或用户/第三方要扩展）再正式做插件系统。写进文档以防将来重构把口子焊死。

---

## 图查询价值验证实验（2026-08-14 加入，待执行）

**问题（诚实评估）**：三实例双跑证明的是"效率优化"（门控修复/模块树缩 token/容器化/edit 升级），**不是图的查询智能**。图独有的查询能力（search_function / impact / neighbors）agent 没主动用（pytest-7490 graph_queries=3）。模块树注入是**静态的，任何 agent 拿到同样注入都能路由**——可迁移，非图独有。

**要证明"图查询 > grep"需要两个条件**：
1. **实例条件（grep 误导）**：issue 关键词指向错误/多个文件，真修复在靠调用链才找得到的函数里（grep 命中非目标文件）。
2. **agent 使用图的机制（当前缺失）**：不能只"提供工具让它选"——flash 默认 grep。需要引导机制：
   - grep 结果歧义（多文件命中/零命中）时，注入"建议用 search_function/impact"提示；
   - 或模块树注入更少，逼 agent 必须查询才能定位。

**候选扫描方法**：用本机 swebench_test.parquet 全量扫，找"gold patch 文件不含 issue 关键词"的实例（grep 误导代理）。现有 20 子集无干净候选：
- requests-2148（FTP 10）：socket.error → models.py，中等误导，可试
- pytest-5103（FTP 1）：all/any → rewrite.py，关键词可定位，不算误导
- requests-1963（FTP 7）：点名 resolve_redirects，非模糊

**实验设计**：
- 双跑同一误导实例：swe_agent（含图查询引导机制）vs Claude Code（纯 grep/自由）
- 对比：定位到正确文件的**第几步**（关键指标）、graph_queries 计数、token/时间/FTP
- 证据：工具调用序列里是否出现 search_function/impact + 定位步数差

**风险**：
- SWE-bench 大多关键词可定位，"grep 真失败"实例难找（需全量扫）。
- 即使找到，flash 可能仍不用图查询——引导机制本身可被 Claude Code 复制（不算图独有）。
- 图"更聪明"的价值可能无法在 benchmark 上干净证明——**备选定位**：图 = 便宜结构注入 + 确定性安全围栏（防级联/死循环），查询智能是加分而非决胜。
